export interface NavItem {
  label: string;
  href: string;
}

export interface Feature {
  id: string;
  category: string;
  title: string;
  description: string;
  linkText: string;
}

export interface NewsItem {
  date: string;
  title: string;
  description: string;
  tag: string;
}
